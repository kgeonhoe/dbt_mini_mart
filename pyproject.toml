[project]
name = "dbt-mini-mart"
version = "0.1.0"
description = "Layered dbt mini mart project with DuckDB"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "dbt-duckdb>=1.8,<2.0",
  "duckdb>=1.0,<2.0",
]
